# gui(graphic user interface) 프로그램 만들기
# cui(character user interface) - 명령 프롬프트, 콘솔

#import tkinter 모듈 tkinter.py
from tkinter import *

root = Tk()
root.title("처음 만드는 윈도우")
root.geometry("300x200")
# 버튼 생성
# grid(), pack()
# dddddddbtn = Button(root, text='버튼', font=("맑은고딕, 18"))
btn.pack(row=0,column=0)









root



root.mainloop()